version: '1'
services:
    map_tb.test:
        build: .
        image: pc_new_oa_map_test:latest
        ports:
            - '8095:80'
        volumes:
            - '.:/var/www/html'
        networks:
            - sail
networks:
    sail:
        driver: bridge
volumes:
    sailredis:
        driver: local
